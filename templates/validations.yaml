{{- if and (.Values.EXTENDED_INSTALL_ON_SENSOR) (not (default false .Values.disableValidation)) }}
{{ include "datafy-agent.validation.mode" . }}
{{ include "datafy-agent.validation.token" . }}
{{- end }}
