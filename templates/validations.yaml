{{- if and (eq (include "datafy-agent.extendedInstallEnabled" .) "true") (not (default false .Values.disableValidation)) }}
{{ include "datafy-agent.validation.mode" . }}
{{ include "datafy-agent.validation.token" . }}
{{- end }}
